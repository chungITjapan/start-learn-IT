let number = Number(prompt('nhap vao 1 so'));
if (number%2==0) {
    console.log(number + ' la so le');
    alert('so vua nhap la so le');
}else {
    console.log(number+' la so chan');
    alert('so vua nhap la so chan');
};